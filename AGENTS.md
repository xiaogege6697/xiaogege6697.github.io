# 仓库维护规则

开始工作前先读 `GOAL.md`，再检查真实代码、当前 Git 状态、线上页面和 GitHub 最新公开仓库。

1. 每轮围绕一个主要变化自主实现，只修改目标所需文件，不做无关重构。
2. 更新项目数据使用 `npm run fetch`，不得编造或保留已删除的仓库信息。
3. 修改后运行 `npm run validate`，并按 `GOAL.md` 检查真实页面、基准对比和五项评分。
4. 未经用户确认，不 push、不部署、不改变自定义域名配置。

规则只守底线；目标负责方向；验收结果决定是否发布。
